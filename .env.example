# URL to the Laravel backend API. Include the /api segment when pointing directly
# to the server (e.g. http://localhost:8000/api). When running `npm run dev` we
# use a Vite proxy so a relative path (`/api`) works even if this is unset.
VITE_API_URL=http://127.0.0.1:8000/api